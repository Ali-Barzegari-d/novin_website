# شروع از اینجا

این پوشه یک بسته‌ی اجرایی برای سپردن کامل پروژه به Codex با مدل `GPT-5.6 terra` است. هدف این است که Agent پس از بازشدن مخزن، بدون پرسش تکمیلی، از R0 تا R5 پیش برود، در پایان هر مرحله QA انجام دهد و commit بسازد.

## دستور شروع برای انسان

1. همه‌ی فایل‌های این بسته را در ریشه‌ی یک repository خالی قرار دهید.
2. Codex را از همان ریشه با مدل `GPT-5.6 terra` باز کنید.
3. فقط بگویید: «طبق AGENTS.md پروژه را از ابتدا تا آخر پیاده‌سازی کن.»

Agent باید `AGENTS.md` را خودکار بخواند. اگر محیط این کار را نکرد، متن بالا را همراه با عبارت «ابتدا AGENTS.md و START_HERE.md را کامل بخوان» ارسال کنید.

## ترتیب اعتبار اسناد

در صورت تعارض، این ترتیب حاکم است:

1. امنیت، جلوگیری از ازدست‌رفتن داده و الزامات قانونی اجباری
2. `DECISIONS.md` (حل تعارض پاسخ‌ها با PRD)
3. `docs/PRD.md` و نسخه Word اصلی
4. `docs/PRODUCT_DISCOVERY.md` (پرامپت، سؤال‌ها و پاسخ‌های مالک محصول)
5. `PLAN.md`، `ARCHITECTURE.md`، `DESIGN.md`، `CONTENT.md` و `DEPLOYMENT.md`

## نکته مهم درباره «بدون سؤال»

نبود اطلاعات ثبتی، متن حقوقی تأییدشده، credential پیامک، SMTP یا درگاه پرداخت نباید توسعه را متوقف کند. Agent باید adapter واقعی، mock امن برای dev/demo و Production Gate صریح بسازد. با این حال نباید R5 را به‌دروغ «آماده تولید» اعلام کند تا gateهای ضروری بسته شوند.

## خروجی مورد انتظار

- سایت فارسی RTL و واکنش‌گرا با UI رسمی، مدرن و متمایز
- فرانت و بک‌اند مستقل در یک monorepo
- پنل کاربر، پنل داخلی، CMS، OTP، درخواست، پیشنهاد، پرداخت mock/بانکی، اعلان و ممیزی
- Docker Compose با bind mount، Nginx میزبان و تنها پورت عمومی 3050
- GitHub Actions، تست‌های unit/integration/E2E/a11y/security و QA بصری
- commit مستقل و گزارش عبور برای R1، R2، R3، R4 و R5

فایل `PROGRESS.md` دفتر ثبت اجرای Agent است و باید در طول کار به‌روز شود.

---
version: 1
slug: "apps-web-src-app-page-tsx"
primary_target: "apps/web/src/app/page.tsx"
related_targets: []
---

# Surface brief — Home

- Audience: مدیرعامل/مالی، فناوری/تحول و عمومی/دولتی با اولویت برابر.
- Outcome: کاربر مسئله سازمانی مبهم را بدون انتخاب خدمت ازپیش‌تعریف‌شده شرح دهد.
- First viewport: تیتر تأییدشده در سمت راست، نقشه فرایند در سمت چپ، CTA اصلی، پیوند فرایند و نوار سه‌گانه اعتماد.
- Story: تشخیص مسئله → نقاط شروع مخاطبان → مرزهای میان‌واحدی → نقشه یکپارچه → فرایند همکاری → نمونه ساختگی → ترکیب تخصص → CTA.
- Visual authority: owner-approved `.impeccable/mocks/decision/canon.png`, decision seed `3701d870`.
- Boundaries: بدون قیمت عمومی، وعده نتیجه، لوگو/پروژه/تیم جعلی یا دریافت داده حساس. placeholderها آشکار و production fail-closed هستند.
- Responsive: RTL واقعی و بدون اسکرول افقی در 320، 768 و 1440؛ در موبایل هیرو تک‌ستونه و ناوبری drawer می‌شود.
- Motion: آشکارسازی بخش‌ها، پیشرفت اسکرول، تعامل نقشه و فشار CTA؛ همه با `prefers-reduced-motion` خاموش می‌شوند.

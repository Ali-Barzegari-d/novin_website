---
name: "شرکت طراحی و تحلیل مالی نوین ایرانیان"
description: "سامانه‌ای روشن و قابل‌ردیابی برای تبدیل پیچیدگی سازمانی به تصمیم و مسیر اجرا"
colors:
  institutional-navy: "#0f2742"
  slate-ink: "#28415d"
  operational-teal: "#087f82"
  operational-teal-dark: "#056669"
  teal-mist: "#e8f5f4"
  technical-ice: "#f4f8fa"
  paper: "#ffffff"
  structural-line: "#cedbe2"
  structural-line-dark: "#8da4b2"
  success: "#147455"
  warning: "#8a5a00"
  danger: "#a12d37"
  focus: "#0d6efd"
typography:
  display:
    fontFamily: "Noto Sans Arabic Variable, Tahoma, sans-serif"
    fontSize: "clamp(2.7rem, 4.2vw, 4.4rem)"
    fontWeight: 900
    lineHeight: 1.2
    letterSpacing: "-0.038em"
  headline:
    fontFamily: "Noto Sans Arabic Variable, Tahoma, sans-serif"
    fontSize: "clamp(2rem, 3.8vw, 4.3rem)"
    fontWeight: 880
    lineHeight: 1.28
    letterSpacing: "-0.032em"
  title:
    fontFamily: "Noto Sans Arabic Variable, Tahoma, sans-serif"
    fontSize: "1.36rem"
    fontWeight: 800
    lineHeight: 1.45
  body:
    fontFamily: "Noto Sans Arabic Variable, Tahoma, sans-serif"
    fontSize: "1rem"
    fontWeight: 400
    lineHeight: 1.75
  label:
    fontFamily: "Noto Sans Arabic Variable, Tahoma, sans-serif"
    fontSize: "0.86rem"
    fontWeight: 730
    lineHeight: 1.7
rounded:
  xs: "4px"
  sm: "6px"
  md: "8px"
  control: "9px"
  action: "10px"
  panel: "14px"
  pill: "999px"
  circle: "50%"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "24px"
  2xl: "28px"
  3xl: "42px"
  4xl: "54px"
components:
  button-primary:
    backgroundColor: "{colors.operational-teal}"
    textColor: "{colors.paper}"
    typography: "{typography.label}"
    rounded: "{rounded.action}"
    padding: "12px 23px"
    height: "50px"
  button-primary-hover:
    backgroundColor: "{colors.operational-teal-dark}"
    textColor: "{colors.paper}"
    rounded: "{rounded.action}"
    padding: "12px 23px"
    height: "50px"
  button-secondary:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.institutional-navy}"
    typography: "{typography.label}"
    rounded: "{rounded.md}"
    padding: "8px 15px"
    height: "42px"
  input-default:
    backgroundColor: "#fbfdfe"
    textColor: "{colors.institutional-navy}"
    typography: "{typography.body}"
    rounded: "{rounded.control}"
    padding: "11px 14px"
    height: "52px"
  card:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.institutional-navy}"
    rounded: "{rounded.panel}"
    padding: "clamp(24px, 4vw, 54px)"
  chip-placeholder:
    backgroundColor: "#fff5d7"
    textColor: "{colors.warning}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
    padding: "4px 10px"
---

# Design System: شرکت طراحی و تحلیل مالی نوین ایرانیان

## Overview

**Creative North Star: "The Traceable Decision Room"**

این جهان بصری مانند اتاق تصمیمی روشن و منظم عمل می‌کند: کاغذ سفید و فضای منفی برای فهم، سرمه‌ای برای وزن نهادی، فیروزه‌ای برای اقدام و خطوط فنی برای نشان‌دادن رابطه‌ها. ظاهر باید حرفه‌ای، دقیق و آرام بماند، اما از سادگی کم‌مایه دور باشد؛ ریتم بزرگ تیترها، سطوح حساب‌شده و «نقشه زنده مسئله تا نتیجه» شخصیت متمایز آن را می‌سازند.

مرجع تصویری تأییدشده، `.impeccable/mocks/decision/canon.png` با seed تصمیم `3701d870` است. جهت نهایی از ترکیب‌بندی دو ستونه، تیتر فارسی راست‌چین، CTA روشن، دیاگرام فرایند و نوار اعتماد آن پیروی می‌کند، اما خود تصویر فقط مرجع داخلی است و دارایی قابل انتشار نیست. تیتر اصلی ثابت و تأییدشده است: «پیچیدگی‌های مالی و کسب‌وکاری را به فرایند، سامانه و محصول قابل‌اجرا تبدیل می‌کنیم.»

**Key Characteristics:**

- RTL فارسی واقعی با سلسله‌مراتب قاطع و فضای تنفس زیاد.
- سفید روشن، سرمه‌ای سازمانی و فیروزه‌ای عملیاتی با رنگ‌های وضعیت محدود و معنایی.
- خطوط یک‌پیکسلی، شبکه‌های فنی و اتصال‌های فرایندی به‌جای تزئینات آزاد.
- عمق کم‌صدا و کنترل‌شده؛ بدون گرادیان نمایشی، شیشه‌گرایی یا جلوه‌های پرزرق‌وبرق.
- حرکت هدفمند برای پیشرفت، ارتباط و بازخورد؛ همیشه قابل حذف با تنظیم کاهش حرکت.

## Colors

پالت مانند یک سند اجرایی معتبر است: کاغذ روشن زمینه را آرام نگه می‌دارد، سرمه‌ای مسئولیت و اعتبار را حمل می‌کند و فیروزه‌ای فقط نقاط اقدام، اتصال و پیشرفت را روشن می‌کند.

### Primary

- **سرمه‌ای نهادی:** رنگ متن اصلی، بخش‌های تمام‌عرض تیره، هسته نمودارها، ناوبری داخلی و نقاطی است که باید وزن و ثبات داشته باشند.
- **فیروزه‌ای عملیاتی:** رنگ CTA اصلی، پیوندهای فرایندی، وضعیت فعال، آیکون‌های کلیدی و نشانه پیشرفت است.
- **فیروزه‌ای عمیق:** فقط برای hover، پیوند تأکیدی و متن روی زمینه‌های روشن استفاده می‌شود.

### Secondary

- **مه فیروزه‌ای:** سطح توضیحی کم‌تنش، حالت انتخاب‌شده و زمینه بخش‌هایی است که همکاری چندتخصصی را نشان می‌دهند.
- **یخ فنی:** سطح خنثی فرم، ردیف hover، callout امنیتی و لایه دوم پس‌زمینه است.

### Neutral

- **کاغذ روشن:** بوم اصلی، سطح کارت و زمینه کنترل در حالت focus است.
- **جوهر اسلیتی:** متن توضیحی پررنگ و ناوبری ثانویه را نگه می‌دارد.
- **خط ساختاری:** مرز کارت، تقسیم‌کننده، جدول، ورودی و اتصال بصری سطوح است؛ نسخه تیره‌تر برای شروع فهرست‌ها و مرزهای مهم به کار می‌رود.

### Named Rules

**The Operational Teal Rule.** فیروزه‌ای نشانه عمل یا اتصال است؛ آن را به رنگ پرکننده همه سطوح یا تزئین بی‌هدف تبدیل نکنید.

**The Honest Status Rule.** سبز، کهربایی و قرمز فقط برای موفقیت، هشدار و خطا هستند و نباید نقش برند یا دکور بگیرند.

## Typography

**Display Font:** Noto Sans Arabic Variable با fallback تاهوما و sans-serif

**Body Font:** Noto Sans Arabic Variable با fallback تاهوما و sans-serif

**Character:** یک خانواده فارسی واحد، مدرن و خوانا، با وزن‌های متغیر کار تمایز را انجام می‌دهد. تیترها فشرده و مقتدرند؛ متن بدنه باز، آرام و توضیحی است.

### Hierarchy

- **Display:** بسیار سنگین، سیال و فشرده؛ فقط برای تیتر اصلی صفحات و پیام‌های تصمیم‌ساز.
- **Headline:** تیترهای بخش با وزن قاطع و ریتم عمودی باز؛ در هر بخش یک ایده را حمل می‌کند.
- **Title:** عنوان کارت، ردیف یا زیرسامانه؛ کوتاه و مستقیم.
- **Body:** متن پایه با ارتفاع خط سخاوتمندانه؛ پاراگراف‌های توضیحی روی عرض محدود می‌مانند تا اسکن‌پذیر باشند.
- **Label:** برچسب فرم، ناوبری و metadata با اندازه کوچک‌تر و وزن بالاتر؛ حروف لاتین و شناسه‌ها در صورت نیاز `dir="ltr"` می‌گیرند.

### Named Rules

**The Persian First Rule.** متن رابط، اعداد نمایشی و تاریخ‌ها فارسی و راست‌به‌چپ‌اند؛ شناسه فنی، شماره همراه و کد فقط در محفظه LTR قرار می‌گیرند.

**The One Statement Rule.** تیتر نمای اول باید همان متن تأییدشده مالک باشد و با شکستن بی‌قاعده، متن تزئینی یا ادعای تبلیغاتی رقابت نکند.

## Layout

پوسته اصلی در عرض زیاد تا ۱۴۴۰ پیکسل رشد می‌کند و با حاشیه افقی ۳۲ پیکسلی در هر سمت بسته می‌شود؛ زیر ۱۱۸۰ پیکسل عرض پوسته به حداکثر ۱۱۰۰ و حاشیه ۲۰ پیکسلی می‌رسد، زیر ۸۶۰ پیکسل حاشیه ۱۶ پیکسلی و زیر ۵۶۰ پیکسل حاشیه ۱۲ پیکسلی دارد. فاصله عمودی بخش‌های اصلی سیال و بزرگ است و معمولاً بین ۸۰ تا ۱۴۸ پیکسل تغییر می‌کند.

در دسکتاپ، هیرو، بخش‌های مسئله، نمونه، تیم و CTA از شبکه‌های دو ستونه نامتقارن استفاده می‌کنند. در عرض ۸۶۰ پیکسل و کمتر، همه این شبکه‌ها تک‌ستونه می‌شوند، عناصر sticky به جریان عادی برمی‌گردند و نوار اعتماد و کنترل‌های نقشه عمودی می‌شوند. در عرض ۵۶۰ پیکسل و کمتر، CTAها تمام‌عرض، فیلدهای دوتایی تک‌ستونه و تراکم داخلی کارت‌ها کمتر می‌شود. در ۳۲۰، ۷۶۸ و ۱۴۴۰ پیکسل هیچ اسکرول افقی در مسیرهای اصلی مجاز نیست.

**The RTL Geometry Rule.** ترتیب دیداری، مبدأ حرکت، underline ناوبری، جدول‌ها و مسیر دیاگرام باید از راست آغاز شوند؛ RTL صرفاً یک ویژگی متن نیست.

**The One Map Rule.** اتصال مالی، فرایند، داده و سامانه با همان زبان نقطه، خط و چرخه بیان می‌شود؛ برای هر بخش استعاره تصویری تازه نسازید.

## Elevation & Depth

عمق یک ترکیب محدود از لایه‌بندی رنگی، مرز یک‌پیکسلی و سایه محیطی سرد است. کارت‌های اصلی و نقشه روی کاغذ با سایه نرم شناور می‌شوند، کارت‌های فضای کاری سایه کم‌تری دارند و بیشتر ساختاری‌اند؛ نوارها و فهرست‌ها عموماً مسطح می‌مانند. backdrop blur فقط در هدر چسبان برای حفظ خوانایی هنگام اسکرول مجاز است.

### Shadow Vocabulary

- **Panel ambient:** سایه وسیع و کم‌کنتراست برای نقشه، فرم اصلی و کارت نمونه (`0 18px 44px rgba(15, 39, 66, .09)`).
- **Workspace quiet:** سایه سبک برای کارت‌های حساب، پیشنهاد و پرداخت (`0 10px 32px rgba(15, 39, 66, .05)`).
- **Action lift:** سایه رنگی کم‌حجم CTA در حالت عادی و hover (`0 8px 24px rgba(8, 127, 130, .16)` تا `0 13px 30px rgba(8, 127, 130, .22)`).

### Named Rules

**The Bordered Before Elevated Rule.** ابتدا با سطح، مرز و فاصله سلسله‌مراتب بسازید؛ سایه فقط برای سطوح تعاملی یا محفظه‌های اصلی اضافه می‌شود.

## Shapes

زبان فرم از گوشه‌های کنترل‌شده و هندسه فنی ساخته شده است: پنل‌های اصلی گوشه ۱۴ پیکسلی، CTAها ۱۰ پیکسلی، ورودی‌ها ۹ پیکسلی و کنترل‌های کوچک ۶ تا ۸ پیکسلی دارند. دایره فقط برای مرحله، وضعیت، آواتار و گره نمودار است؛ pill فقط برای برچسب کوتاه و وضعیت به کار می‌رود. خطوط و borderها همیشه باریک‌اند و نباید با قاب‌های ضخیم یا گوشه‌های بیش از حد نرم جایگزین شوند.

## Components

### Buttons

- **Shape:** مستطیل حرفه‌ای با گوشه کنترل‌شده، حداقل ارتفاع ۵۰ پیکسل و فضای لمس کافی.
- **Primary:** فیروزه‌ای عملیاتی روی متن سفید؛ وزن بالا، پیکان در جهت جریان RTL و نسخه بزرگ با حداقل ارتفاع ۵۸ پیکسل.
- **Hover / Focus:** hover به فیروزه‌ای عمیق و سایه کمی بلندتر می‌رود؛ پیکان ۴ پیکسل در جهت حرکت جابه‌جا می‌شود. focus-visible همیشه outline آبی ۳ پیکسلی با فاصله ۴ پیکسل دارد.
- **Secondary / Inverse:** دکمه ثانویه کاغذی با خط ساختاری تیره است؛ نسخه معکوس روی زمینه سرمه‌ای سفید است و در hover به فیروزه‌ای تبدیل می‌شود.
- **Disabled:** opacity کاهش می‌یابد، سایه حذف می‌شود و cursor وضعیت غیرفعال را نشان می‌دهد.

### Chips

- **Style:** pillهای کوچک فقط برای وضعیت، نقش یا هشدار انتشار هستند. placeholder با زمینه کهربایی روشن و متن هشدار، نقش داخلی با فیروزه‌ای شفاف روی سرمه‌ای، و stateها با رنگ معنایی نمایش داده می‌شوند.
- **State:** متن کوتاه، بدون آیکون تزئینی و بدون استفاده به‌عنوان CTA.

### Cards / Containers

- **Corner Style:** پنل اصلی با گوشه ۱۴ پیکسلی؛ کارت‌های کوچک‌تر ۱۰ تا ۱۲ پیکسلی.
- **Background:** کاغذ روشن روی بوم سفید یا یخ فنی؛ بخش‌های توضیحی می‌توانند مه فیروزه‌ای باشند.
- **Shadow Strategy:** کارت‌های اصلی از Panel ambient و فضای کاری از Workspace quiet استفاده می‌کنند؛ فهرست‌ها با خط جدا می‌شوند، نه کارت تکراری.
- **Border:** خط ساختاری یک‌پیکسلی، با نسخه تیره‌تر برای مرزهای آغازین و جمع‌بندی مالی.
- **Internal Padding:** از ۲۴ پیکسل در موبایل تا ۴۴ یا ۵۴ پیکسل در دسکتاپ.

### Inputs / Fields

- **Style:** زمینه یخ‌مانند، خط خنثی، گوشه ۹ پیکسلی، حداقل ارتفاع ۵۲ پیکسل و label مستقل بالای کنترل.
- **Focus:** زمینه سفید، border فیروزه‌ای و حلقه شفاف ۳ پیکسلی؛ outline سراسری صفحه‌کلید نیز حفظ می‌شود.
- **Error / Disabled:** خطا با متن قرمز روی سطح صورتی بسیار روشن و border هماهنگ؛ غیرفعال کم‌رنگ اما همچنان خوانا.

### Navigation

هدر عمومی چسبان، نیمه‌شفاف و ۸۶ پیکسلی است؛ لینک فعال یا hover یک خط فیروزه‌ای باریک از مبدأ راست می‌گیرد. زیر ۱۱۸۰ پیکسل ناوبری اصلی با دکمه menu جایگزین می‌شود و drawer تمام‌ارتفاع با focus trap، Escape و بازگردانی focus کار می‌کند. ناوبری داخلی سرمه‌ای، متراکم و نقش‌محور است و active state آن با سطح سفید شفاف مشخص می‌شود.

### Live Problem Map

الگوی امضای محصول یک پنل فنی با شبکه کم‌رنگ، مسیر پیوسته، گره‌های تعاملی و برچسب‌های دقیق است. در موبایل برچسب‌های شناور به legend سه‌ستونه تبدیل می‌شوند. حالت فعال هم‌زمان اندازه گره، سطح کنترل و توضیح زنده را تغییر می‌دهد؛ این الگو داده واقعی مشتری را وانمود نمی‌کند.

حرکت در کل سیستم باید از easing خروج نرم (`cubic-bezier(0.16, 1, 0.3, 1)`) استفاده کند: گذار حالت‌ها ۲۰۰ تا ۳۵۰ میلی‌ثانیه، رسم مسیر حداکثر ۱٫۴ ثانیه و press با spring کوتاه. با `prefers-reduced-motion: reduce` همه transitionها و animationها عملاً خاموش می‌شوند و معنا بدون حرکت باقی می‌ماند.

## Do's and Don'ts

### Do:

- **Do** تیتر تأییدشده، CTA روشن و نقشه مسئله تا نتیجه را به‌عنوان سه لنگر نمای اول حفظ کنید.
- **Do** سطوح را ابتدا با فاصله، خط یک‌پیکسلی و تضاد تونال سازمان دهید و سایه را محدود نگه دارید.
- **Do** برای هر کنترل حالت hover، focus-visible، active یا pressed، disabled، loading، error و success مرتبط را تعریف کنید.
- **Do** RTL واقعی، ناوبری کامل صفحه‌کلید، WCAG 2.2 AA و کاهش حرکت را در ۳۲۰، ۷۶۸ و ۱۴۴۰ پیکسل کنترل کنید.
- **Do** placeholder و داده ساختگی را آشکار برچسب بزنید و رنگ هشدار آن را از محتوای واقعی متمایز نگه دارید.
- **Do** فقط دارایی‌هایی را منتشر کنید که منبع، مجوز یا رضایت، مالک حقوقی، متن جایگزین و محدودیت استفاده‌شان در `docs/ASSET_SOURCES.md` ثبت شده است.

### Don't:

- **Don't** `canon.png` را در سایت سرو کنید؛ این فایل فقط مرجع ترکیب‌بندی داخلی است.
- **Don't** لوگوی هندسی موقت، تصویر تیم یا پروژه، نشان مشتری یا ادعای قابل انتساب را پیش از تأیید هویت و مجوز انتشار production-ready معرفی کنید.
- **Don't** از گرادیان نمایشی، glassmorphism، تصاویر دست‌دادن استوک، dashboard جعلی، metric ساختگی یا متن ریز استفاده کنید.
- **Don't** قیمت عمومی، وعده نتیجه یا ظاهر تبلیغاتی اغراق‌آمیز را وارد الگوی بصری کنید.
- **Don't** برای جذابیت، رنگ‌های وضعیت را تزئینی یا موشن‌های پراکنده و بی‌ارتباط با پیشرفت و اتصال اضافه کنید.

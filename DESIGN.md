---
name: "اطمینان هدایت‌شده"
status: "approved direction"
approved_at: "۱۴۰۵/۰۶/۱۳"
primary_color: "#0A66C2"
---

# سیستم طراحی «اطمینان هدایت‌شده»

## ایده اصلی

رابط باید یک سازمان پیچیده را به مسیری روشن، قابل‌ردیابی و کم‌ریسک هدایت کند. اعتماد از طریق شفافیت مرحله‌ها، مرز تعهد، زبان دقیق و بازخورد قابل‌فهم ساخته می‌شود؛ نه با ادعا، آمار ساختگی یا تزئینات مالی کلیشه‌ای.

نمای نخست بر یک پیام روشن، اقدام اصلی و چهار مرحله همکاری تمرکز دارد. بخش‌های بعدی تخصص، مسئله، مدل تحلیل، خروجی و نحوه شروع را با سلسله‌مراتب مشخص توضیح می‌دهند.

## رنگ‌ها

| نقش | رنگ | کاربرد |
| --- | --- | --- |
| آبی اعتماد | `#0A66C2` | CTA اصلی، پیوندهای مهم، مرحله فعال و مسیرهای اصلی نمودار |
| آبی عمیق | `#074B91` | Hover و سطوح تعاملی پرفشار |
| سرمه‌ای | `#10233D` | متن اصلی و بخش‌های نهادی تیره |
| فیروزه‌ای | `#008C85` | اتصال، امنیت، پیشرفت و اقدام مکمل |
| سبز موفقیت | `#147455` | فقط وضعیت موفق یا تأیید |
| کرمی | `#F7F1E6` | زمینه آرام، بخش‌های توضیحی و کاهش خشکی رابط |
| طلایی | `#D2A12F` | نقاط تصمیم و تأکید محدود |
| عنابی | `#942B45` | تأکید بسیار کم، یک مرحله یا نشانه مهم |
| سفید | `#FFFFFF` | بوم و سطح کارت |

رنگ‌ها نقش معنایی دارند. عنابی و طلایی نباید به رنگ غالب یا تزئین تکرارشونده تبدیل شوند. گرادیان چندرنگ فقط برای نشان‌دادن پیوستگی چند حوزه مجاز است.

## تایپوگرافی فارسی

- تیتر: `IRANYekanX`, `IRANYekan`, سپس `Vazirmatn Variable` به‌عنوان fallback.
- متن و کنترل: `Vazirmatn Variable`.
- وزیرمتن از بسته self-hosted و دارای مجوز OFL داخل build بارگذاری می‌شود.
- ایران‌یکان فقط پس از تحویل فایل‌های وب دارای مجوز مالک پروژه فعال می‌شود؛ نبود فایل نباید با نسخه غیرمجاز جبران شود.
- همه متن‌های عمومی فارسی و RTL هستند.
- اعداد نمایشی، مرحله‌ها، تاریخ و مبلغ برای کاربر فارسی می‌شوند. شناسه فنی، ایمیل و داده‌ای که باید کپی شود می‌تواند در محفظه LTR باقی بماند.

## چیدمان

- نمای نخست در دسکتاپ مرکزی و قاطع است؛ تیتر، توضیح، CTA و مرز تعهد قبل از چهار مرحله همکاری دیده می‌شوند.
- مسیر همکاری در چهار کارت به ترتیب RTL نمایش داده می‌شود.
- در تبلت کارت‌ها دو ستونه و در موبایل تک‌ستونه می‌شوند.
- محتوای تحلیلی از فضای منفی، خطوط یک‌پیکسلی و سطح‌های روشن استفاده می‌کند.
- گوشه‌ها کنترل‌شده‌اند: `۸–۱۲px` برای کنترل و `۱۴–۲۲px` برای پنل‌های اصلی.
- هیچ مسیر اصلی در عرض‌های `۳۲۰`، `۷۶۸` و `۱۴۴۰` پیکسل نباید اسکرول افقی داشته باشد.

## حرکت

حرکت باید رابطه و پیشرفت را توضیح دهد:

- ورود مرحله‌ای تیتر، توضیح، CTA و یادداشت اعتماد.
- Stagger کارت‌های مسیر همکاری از راست به چپ.
- رسم مسیرهای SVG در نمودار تحلیل هنگام ورود به viewport.
- پاسخ فنری کوتاه برای دکمه‌ها، کارت‌ها و نقطه فعال نمودار.
- نمایش کنترل‌شده بخش‌ها فقط یک بار و بدون scroll hijacking یا parallax نمایشی.
- انیمیشن فقط با `opacity` و `transform` انجام می‌شود.
- در `prefers-reduced-motion` زمان حرکت حذف می‌شود و محتوا از ابتدا قابل مشاهده است.

## نمودارها

- نمودار تزئینی یا دارای داده ساختگی ممنوع است.
- نمودار مفهومی باید با برچسب صریح «نمایش مفهومی» مشخص شود.
- نقشه تبدیل مسئله یک جریان همگرایی است: یک مسئله سازمانی از پنج زاویه موازی مالی، فرایند، داده، سامانه و محصول بررسی می‌شود و به یک تصمیم قابل اجرا می‌رسد؛ حلقه یا توالی ساختگی میان این لایه‌ها نمایش داده نمی‌شود.
- جهت حرکت و فلش‌ها در RTL بررسی می‌شوند.
- رنگ‌های مالی، فرایند، داده، سامانه و خروجی ثابت و قابل تفکیک باقی می‌مانند.
- نسخه موبایل باید برچسب‌های خوانا داشته باشد و وابسته به Hover نباشد.

## سیاست تصویر

جهت تأییدشده بدون عکس استوک است. هویت بصری با تایپوگرافی فارسی، نمودارهای تألیفی، خطوط فرایندی، رنگ‌های معنایی، فضای منفی و حرکت هدفمند ساخته می‌شود. تصاویر ترید، ارز دیجیتال، پول نقد، جلسات عمومی استوک، فلش صعودی و داشبوردهای غیرواقعی وارد رابط نمی‌شوند.

اگر در آینده تصویر واقعی شرکت، تیم یا پروژه فراهم شد، استفاده از آن فقط پس از تأیید مالک، رضایت انتشار و ثبت منبع و مجوز در `docs/ASSET_SOURCES.md` مجاز است.

## قواعد اعتماد و دسترس‌پذیری

- هیچ مشتری، آمار، گواهی، پروژه یا نتیجه‌ای بدون مدرک نمایش داده نمی‌شود.
- مرز بررسی اولیه، پیشنهاد اختصاصی و قرارداد پروژه اصلی صریح می‌ماند.
- کنتراست، Focus، ترتیب صفحه‌کلید و هدف لمسی حداقل `۴۴px` حفظ می‌شوند.
- معنا فقط با رنگ منتقل نمی‌شود؛ متن، آیکون یا شکل نیز همراه آن است.
- فرم‌ها خطا، حالت در حال انجام، موفقیت و امکان تلاش دوباره دارند.
